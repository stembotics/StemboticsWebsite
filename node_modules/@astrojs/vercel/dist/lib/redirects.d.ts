import type { AstroConfig, IntegrationResolvedRoute } from 'astro';
interface VercelRoute {
    src: string;
    methods?: string[];
    dest?: string;
    headers?: Record<string, string>;
    status?: number;
    continue?: boolean;
}
export declare function escapeRegex(content: string): string;
export declare function getRedirects(routes: IntegrationResolvedRoute[], config: AstroConfig): VercelRoute[];
export {};
