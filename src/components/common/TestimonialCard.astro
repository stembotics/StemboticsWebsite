---
export interface Testimonial {
  id: number;
  content: string;
  author: string;
  role: string;
  image: string;
}

interface Props {
  testimonial: Testimonial;
}

const { testimonial } = Astro.props;
---

<div class="bg-white rounded-lg shadow p-6 flex flex-col h-full">
  <div class="mb-4">
    <svg class="h-8 w-8 text-primary-400" fill="currentColor" viewBox="0 0 24 24">
      <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
    </svg>
  </div>
  <p class="text-slate-700 flex-grow italic">"{testimonial.content}"</p>
  <div class="mt-6 flex items-center">
    <img 
      src={testimonial.image} 
      alt={testimonial.author} 
      class="h-10 w-10 rounded-full object-cover"
    />
    <div class="ml-3">
      <p class="text-sm font-medium text-slate-900">{testimonial.author}</p>
      <p class="text-xs text-slate-500">{testimonial.role}</p>
    </div>
  </div>
</div>